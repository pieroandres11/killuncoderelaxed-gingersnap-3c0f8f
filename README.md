# Mi página

Una página HTML estática con contenido de texto, desplegada en Netlify.

## Tecnologías

- HTML5 / CSS3 (sin dependencias externas)

## Ejecutar localmente

Abre `index.html` directamente en el navegador, o sirve la carpeta con cualquier servidor HTTP estático:

```bash
npx serve .
```
